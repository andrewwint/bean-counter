# Node
node_modules/
dist/
*.log

# Environment — .env.example is committed on purpose, real .env files are not.
.env
.env.local
.env.*.local
!.env.example

# Python / analytics
__pycache__/
.venv/
.ipynb_checkpoints/
analytics/data/*.parquet
# Exports carry real shop data. Ignore every generated shape, not just the one
# a script happens to write today.
analytics/data/*.csv

# Infra
cdk.out/

# OS
.DS_Store

# NOTE: .agents/ is deliberately NOT ignored — the run trail is a deliverable.
